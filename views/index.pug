extends layout

block content
  p(class='top') It has been...
  p(class='count')= lastReset
  p(class='bottom') Days since our last production defect

  p(class='footer') Routes | reset: POST /reset | set: POST /setDays | get last reset: GET /lastReset